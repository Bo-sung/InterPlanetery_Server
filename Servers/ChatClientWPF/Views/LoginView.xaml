<UserControl x:Class="ChatClientWPF.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{StaticResource BackgroundBrush}">
        <Border Background="{StaticResource SurfaceBrush}" 
                BorderBrush="{StaticResource BorderBrush}" 
                BorderThickness="1" 
                CornerRadius="4"
                HorizontalAlignment="Center" 
                VerticalAlignment="Center" 
                Padding="30" Width="400">
            <StackPanel>
                <TextBlock Text="인터플래니터리 로그인" Style="{StaticResource PageHeaderStyle}" HorizontalAlignment="Center"/>
                
                <TextBlock Text="서버 IP" Style="{StaticResource LabelStyle}" Margin="0,0,0,5"/>
                <TextBox Text="{Binding ServerIp}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,15"/>

                <TextBlock Text="서버 포트" Style="{StaticResource LabelStyle}" Margin="0,0,0,5"/>
                <TextBox Text="{Binding ServerPort}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,15"/>

                <TextBlock Text="사용자명" Style="{StaticResource LabelStyle}" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Username}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,15"/>

                <TextBlock Text="비밀번호" Style="{StaticResource LabelStyle}" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Password}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,20"/>

                <Button Content="로그인" Command="{Binding LoginCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,0,10"/>
                <Button Content="회원가입" Command="{Binding RegisterCommand}" Style="{StaticResource SecondaryButtonStyle}"/>

                <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource AccentBrush}"
                           HorizontalAlignment="Center" Margin="0,15,0,0" FontSize="12"/>

                <ProgressBar IsIndeterminate="True" Height="2" Margin="0,10,0,0"
                             Visibility="{Binding IsConnecting, Converter={StaticResource BoolToVisibilityConverter}}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
