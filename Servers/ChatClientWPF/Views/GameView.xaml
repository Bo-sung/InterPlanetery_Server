<UserControl x:Class="ChatClientWPF.Views.GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:ChatClientWPF.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#2D2D30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#3E3E42" Padding="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Game View" Foreground="White" FontSize="16" FontWeight="Bold" Margin="0,0,20,0"/>
                <TextBlock Text="{Binding GameStatus}" Foreground="#007ACC" FontSize="14" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Game Area (Placeholder) -->
        <Border Grid.Row="1" Margin="10" BorderBrush="#555" BorderThickness="1" Background="Black">
            <Canvas ClipToBounds="True">
                <TextBlock Text="Game Rendering Area" Foreground="Gray" Canvas.Left="10" Canvas.Top="10"/>
                <TextBlock Text="{Binding ServerTick, StringFormat='Server Tick: {0}'}" Foreground="Lime" Canvas.Right="10" Canvas.Top="10"/>
            </Canvas>
        </Border>

        <!-- Footer / Controls -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="Leave Game" Command="{Binding LeaveGameCommand}" Padding="10,5" Background="#C42B1C" Foreground="White" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</UserControl>
