<UserControl x:Class="ChatClientWPF.Views.LobbyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Background="{StaticResource BackgroundBrush}" Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>

        <!-- Room List Section -->
        <Border Grid.Column="0" Style="{StaticResource SectionBorderStyle}" Margin="0,0,20,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="게임 방 목록" Style="{StaticResource SectionHeaderStyle}"/>
                    <Button Grid.Column="1" Content="새로고침" Command="{Binding RefreshCommand}" Style="{StaticResource SecondaryButtonStyle}" Padding="15,5"/>
                </Grid>

                <ListBox Grid.Row="1" ItemsSource="{Binding RoomList}" Style="{StaticResource ListBoxStyle}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource BackgroundBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding RoomName}" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,5"/>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding PlayerCount, StringFormat='인원: {0}'}" Style="{StaticResource TextSecondaryStyle}" Margin="0,0,5,0"/>
                                            <TextBlock Text="/" Style="{StaticResource TextSecondaryStyle}" Margin="0,0,5,0"/>
                                            <TextBlock Text="{Binding MaxPlayers}" Style="{StaticResource TextSecondaryStyle}" Margin="0,0,15,0"/>
                                            <TextBlock Text="{Binding MapID, StringFormat='맵: {0}'}" Style="{StaticResource TextSecondaryStyle}"/>
                                        </StackPanel>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                        <Border Background="{StaticResource SuccessBrush}" CornerRadius="4" Padding="8,4" Margin="0,0,10,0">
                                            <TextBlock Text="{Binding RoomState}" Foreground="White" FontSize="12" FontWeight="Bold"/>
                                        </Border>
                                        <Button Content="입장" 
                                                Command="{Binding DataContext.JoinRoomCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                CommandParameter="{Binding RoomId}"
                                                Style="{StaticResource PrimaryButtonStyle}" Padding="15,5"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <!-- Right Panel -->
        <StackPanel Grid.Column="1">
            <!-- Create Room -->
            <Border Style="{StaticResource SectionBorderStyle}" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="방 만들기" Style="{StaticResource SectionHeaderStyle}"/>
                    
                    <TextBlock Text="방 제목" Style="{StaticResource LabelStyle}" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding NewRoomName}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,15"/>

                    <TextBlock Text="맵 ID" Style="{StaticResource LabelStyle}" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding SelectedMapId}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,15"/>

                    <CheckBox Content="비공개 방" IsChecked="{Binding IsPrivate}" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,15"/>

                    <Button Content="방 생성" Command="{Binding CreateRoomCommand}" Style="{StaticResource SuccessButtonStyle}" HorizontalAlignment="Stretch"/>
                </StackPanel>
            </Border>

            <!-- User Info -->
            <Border Style="{StaticResource SectionBorderStyle}">
                <StackPanel>
                    <TextBlock Text="내 정보" Style="{StaticResource SectionHeaderStyle}"/>
                    <TextBlock Text="연결됨" Foreground="{StaticResource SuccessBrush}" Margin="0,0,0,15"/>
                    <Button Content="로그아웃" Command="{Binding LogoutCommand}" Style="{StaticResource DangerButtonStyle}" HorizontalAlignment="Stretch"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
