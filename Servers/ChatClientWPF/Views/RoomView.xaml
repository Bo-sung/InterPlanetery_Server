<UserControl x:Class="ChatClientWPF.Views.RoomView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Background="{StaticResource BackgroundBrush}" Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="400"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Grid.ColumnSpan="2" Style="{StaticResource SectionBorderStyle}" Margin="0,0,0,20" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Content="← 로비로 나가기" Command="{Binding LeaveRoomCommand}" Style="{StaticResource SecondaryButtonStyle}" Padding="15,5"/>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="{Binding CurrentRoom.RoomName}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,20,0"/>
                    <Border Background="{StaticResource SuccessBrush}" CornerRadius="4" Padding="8,4" VerticalAlignment="Center">
                        <TextBlock Text="{Binding CurrentRoom.MapID, StringFormat='맵 ID: {0}'}" Foreground="White" FontSize="14"/>
                    </Border>
                </StackPanel>

                <Button Grid.Column="2" Content="{Binding IsReady, Converter={StaticResource BoolToReadyTextConverter}}" 
                        Command="{Binding ReadyCommand}" 
                        Style="{Binding IsReady, Converter={StaticResource BoolToReadyButtonStyleConverter}}" 
                        Padding="20,8"/>
            </Grid>
        </Border>

        <!-- Player Slots (Left) -->
        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,20,0">
            <Border Style="{StaticResource SectionBorderStyle}" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="{Binding CurrentRoom.PlayerCount, StringFormat='플레이어 ({0}/{1})'}" Style="{StaticResource SectionHeaderStyle}"/>
                    <!-- Placeholder for player slots visualization -->
                    <Border Background="{StaticResource BackgroundBrush}" BorderBrush="{StaticResource PrimaryBrush}" BorderThickness="2" CornerRadius="4" Padding="20" Margin="0,0,0,15">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border Width="60" Height="60" CornerRadius="30" Background="{StaticResource PrimaryBrush}" Margin="0,0,15,0">
                                <TextBlock Text="P1" Foreground="White" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="플레이어 1" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock Text="방장" Foreground="{StaticResource TextSecondaryBrush}" FontSize="13"/>
                            </StackPanel>
                            <Border Grid.Column="2" Background="{StaticResource WarningBrush}" CornerRadius="4" Padding="10,5">
                                <TextBlock Text="준비 안됨" Foreground="White" FontWeight="Bold"/>
                            </Border>
                        </Grid>
                    </Border>
                    
                     <Border Background="{StaticResource BackgroundBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="2" CornerRadius="4" Padding="20" Opacity="0.5" IsEnabled="False">
                         <!-- Empty Slot -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Width="60" Height="60" CornerRadius="30" Background="{StaticResource BorderBrush}" Margin="0,0,15,0">
                                <TextBlock Text="?" Foreground="{StaticResource SecondaryBrush}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="플레이어 대기 중..." FontSize="18" FontWeight="Bold" Foreground="{StaticResource SecondaryBrush}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </StackPanel>
            </Border>
            
            <Border Style="{StaticResource SectionBorderStyle}">
                <StackPanel>
                    <TextBlock Text="게임 설정" Style="{StaticResource SectionHeaderStyle}"/>
                     <Grid Margin="0,5">
                        <TextBlock Text="맵" Style="{StaticResource TextSecondaryStyle}"/>
                        <TextBlock Text="3레인 전투장" Style="{StaticResource TextPrimaryStyle}" HorizontalAlignment="Right" FontWeight="Bold"/>
                    </Grid>
                    <Separator Background="{StaticResource BorderBrush}" Margin="0,5"/>
                     <Grid Margin="0,5">
                        <TextBlock Text="최대 인원" Style="{StaticResource TextSecondaryStyle}"/>
                        <TextBlock Text="2명" Style="{StaticResource TextPrimaryStyle}" HorizontalAlignment="Right" FontWeight="Bold"/>
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Chat (Right) -->
        <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource SectionBorderStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Text="채팅" Style="{StaticResource SectionHeaderStyle}"/>
                
                <ListBox Grid.Row="1" ItemsSource="{Binding ChatMessages}" Style="{StaticResource ListBoxStyle}" Margin="0,0,0,15">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,5">
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                    <TextBlock Text="{Binding SenderId}" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" FontSize="13"/>
                                    <!-- <TextBlock Text="{Binding Timestamp}" Foreground="#666" FontSize="11" Margin="10,0,0,0" VerticalAlignment="Bottom"/> -->
                                </StackPanel>
                                <Border Background="{StaticResource SurfaceBrush}" CornerRadius="4" Padding="10,8" BorderBrush="{StaticResource PrimaryBrush}" BorderThickness="3,0,0,0">
                                    <TextBlock Text="{Binding Message}" Foreground="{StaticResource TextPrimaryBrush}" TextWrapping="Wrap"/>
                                </Border>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Text="{Binding ChatInput, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" Margin="0,0,10,0">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding SendChatCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>
                    <Button Grid.Column="1" Content="전송" Command="{Binding SendChatCommand}" Style="{StaticResource PrimaryButtonStyle}"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
