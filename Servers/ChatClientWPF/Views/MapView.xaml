<UserControl x:Class="ChatClientWPF.Views.MapView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="800" d:DesignWidth="1200">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 컨트롤 패널 -->
        <Border Grid.Row="0" Style="{StaticResource SectionBorderStyle}" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Map Data Loader" Style="{StaticResource SectionHeaderStyle}"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Map ID:" Style="{StaticResource LabelStyle}" Margin="0,0,10,0"/>
                    <TextBox Grid.Column="1" x:Name="MapIdTextBox" Text="1" Style="{StaticResource TextBoxStyle}"/>

                    <Button Grid.Column="3" x:Name="RenderMapButton" Content="Load Map from DB" Style="{StaticResource PrimaryButtonStyle}" Click="RenderMapButton_Click"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- 맵 디스플레이 -->
        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="5">
            <Grid>
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <Canvas x:Name="MapCanvas" Background="#FF1C1C1C" Width="2000" Height="1500">
                        <!-- 행성과 경로가 여기에 그려집니다 -->
                    </Canvas>
                </ScrollViewer>

                <!-- 로딩 인디케이터 -->
                <Border x:Name="LoadingPanel"
                        Background="#CC000000"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Visibility="Collapsed">
                    <StackPanel Margin="30" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="로딩 중..."
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   FontSize="16"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,10"/>
                    </StackPanel>
                </Border>

                <!-- 행성 정보 표시 (오른쪽 하단) -->
                <Border x:Name="PlanetInfoPanel"
                        Background="#AA252526"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="5"
                        Padding="15"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Bottom"
                        Margin="20"
                        Visibility="Collapsed"
                        IsHitTestVisible="False">
                    <TextBlock x:Name="PlanetInfoText"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               FontSize="13"
                               FontFamily="Consolas"/>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
